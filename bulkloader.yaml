# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: Mapping
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: domain
      external_name: domain
      # Type: String Stats: 143 properties of this type in this kind.

    - property: namespace
      external_name: namespace
      # Type: String Stats: 143 properties of this type in this kind.


- kind: analytics_credentials
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key("analytics_credentials", key_is_id=True)

    - property: email
      external_name: email
      # Type: String Stats: 1 properties of this type in this kind.

    - property: password
      external_name: password
      # Type: String Stats: 1 properties of this type in this kind.

    - property: source
      external_name: source
      # Type: String Stats: 1 properties of this type in this kind.

    - property: table_id
      external_name: table_id
      # Type: String Stats: 1 properties of this type in this kind.

    - property: token
      external_name: token
      # Type: String Stats: 1 properties of this type in this kind.

    - property: token_created_timestamp
      external_name: token_created_timestamp
      # Type: Date/Time Stats: 1 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')


- kind: auth_user
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: date_joined
      external_name: date_joined
      # Type: Date/Time Stats: 148 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: email
      external_name: email
      # Type: String Stats: 148 properties of this type in this kind.

    - property: first_name
      external_name: first_name
      # Type: String Stats: 148 properties of this type in this kind.

    - property: idxf_email_l_iexact
      external_name: idxf_email_l_iexact
      # Type: String Stats: 148 properties of this type in this kind.

    - property: idxf_username_l_iexact
      external_name: idxf_username_l_iexact
      # Type: String Stats: 148 properties of this type in this kind.

    - property: is_active
      external_name: is_active
      # Type: Boolean Stats: 148 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: is_staff
      external_name: is_staff
      # Type: Boolean Stats: 148 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: is_superuser
      external_name: is_superuser
      # Type: Boolean Stats: 148 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: last_login
      external_name: last_login
      # Type: Date/Time Stats: 148 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: last_name
      external_name: last_name
      # Type: String Stats: 148 properties of this type in this kind.

    - property: password
      external_name: password
      # Type: String Stats: 148 properties of this type in this kind.

    - property: username
      external_name: username
      # Type: String Stats: 148 properties of this type in this kind.


- kind: authentication_activationcode
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: activation_key
      external_name: activation_key
      # Type: String Stats: 23 properties of this type in this kind.

    - property: user_id
      external_name: user_id
      # Type: Integer Stats: 23 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: authentication_userprofile
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: mobile_phone
      external_name: mobile_phone
      # Type: String Stats: 106 properties of this type in this kind.

    - property: title
      external_name: title
      # Type: String Stats: 106 properties of this type in this kind.

    - property: user_id
      external_name: user_id
      # Type: Integer Stats: 106 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: core_applieddesign
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: color
      external_name: color
      # Type: String Stats: 4 properties of this type in this kind.

    - property: created
      external_name: created
      # Type: Date/Time Stats: 549 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: current
      external_name: current
      # Type: Boolean Stats: 549 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: design_id
      external_name: design_id
      # Type: Integer Stats: 549 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: dirty
      external_name: dirty
      # Type: Boolean Stats: 549 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: placeholder
      external_name: placeholder
      # Type: Boolean Stats: 549 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: private
      external_name: private
      # Type: Boolean Stats: 549 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: public
      external_name: public
      # Type: Boolean Stats: 549 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: reference
      external_name: reference
      # Type: String Stats: 549 properties of this type in this kind.

    - property: site_id
      external_name: site_id
      # Type: Integer Stats: 549 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: core_boolean
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: created
      external_name: created
      # Type: Date/Time Stats: 168 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: current
      external_name: current
      # Type: Boolean Stats: 168 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: dirty
      external_name: dirty
      # Type: Boolean Stats: 168 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: placeholder
      external_name: placeholder
      # Type: Boolean Stats: 168 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: private
      external_name: private
      # Type: Boolean Stats: 168 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: public
      external_name: public
      # Type: Boolean Stats: 168 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: reference
      external_name: reference
      # Type: String Stats: 168 properties of this type in this kind.

    - property: site_id
      external_name: site_id
      # Type: Integer Stats: 168 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: value
      external_name: value
      # Type: Boolean Stats: 168 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)


- kind: core_business
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: created
      external_name: created
      # Type: Date/Time Stats: 222 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: current
      external_name: current
      # Type: Boolean Stats: 222 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: description
      external_name: description
      # Type: String Stats: 222 properties of this type in this kind.

    - property: dirty
      external_name: dirty
      # Type: Boolean Stats: 222 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: name
      external_name: name
      # Type: String Stats: 222 properties of this type in this kind.

    - property: placeholder
      external_name: placeholder
      # Type: Boolean Stats: 222 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: private
      external_name: private
      # Type: Boolean Stats: 222 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: public
      external_name: public
      # Type: Boolean Stats: 222 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: reference
      external_name: reference
      # Type: String Stats: 222 properties of this type in this kind.

    - property: site_id
      external_name: site_id
      # Type: Integer Stats: 222 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: core_contact
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: created
      external_name: created
      # Type: Date/Time Stats: 144 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: current
      external_name: current
      # Type: Boolean Stats: 144 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: dirty
      external_name: dirty
      # Type: Boolean Stats: 144 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: email
      external_name: email
      # Type: String Stats: 144 properties of this type in this kind.

    - property: fax
      external_name: fax
      # Type: String Stats: 144 properties of this type in this kind.

    - property: phone
      external_name: phone
      # Type: String Stats: 144 properties of this type in this kind.

    - property: placeholder
      external_name: placeholder
      # Type: Boolean Stats: 144 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: private
      external_name: private
      # Type: Boolean Stats: 144 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: public
      external_name: public
      # Type: Boolean Stats: 144 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: reference
      external_name: reference
      # Type: String Stats: 144 properties of this type in this kind.

    - property: site_id
      external_name: site_id
      # Type: Integer Stats: 144 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: voip
      external_name: voip
      # Type: String Stats: 144 properties of this type in this kind.


- kind: core_date
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: created
      external_name: created
      # Type: Date/Time Stats: 288 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: current
      external_name: current
      # Type: Boolean Stats: 288 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: dirty
      external_name: dirty
      # Type: Boolean Stats: 288 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: placeholder
      external_name: placeholder
      # Type: Boolean Stats: 288 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: private
      external_name: private
      # Type: Boolean Stats: 288 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: public
      external_name: public
      # Type: Boolean Stats: 288 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: reference
      external_name: reference
      # Type: String Stats: 288 properties of this type in this kind.

    - property: site_id
      external_name: site_id
      # Type: Integer Stats: 288 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: value
      external_name: value
      # Type: Date/Time Stats: 288 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')


- kind: core_day
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: _hours
      external_name: _hours
      # Type: String Stats: 759 properties of this type in this kind.

    - property: closed
      external_name: closed
      # Type: Boolean Stats: 759 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: closing
      external_name: closing
      # Type: Date/Time Stats: 759 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: created
      external_name: created
      # Type: Date/Time Stats: 759 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: current
      external_name: current
      # Type: Boolean Stats: 759 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: dirty
      external_name: dirty
      # Type: Boolean Stats: 759 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: opening
      external_name: opening
      # Type: Date/Time Stats: 759 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: placeholder
      external_name: placeholder
      # Type: Boolean Stats: 759 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: private
      external_name: private
      # Type: Boolean Stats: 759 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: public
      external_name: public
      # Type: Boolean Stats: 759 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: reference
      external_name: reference
      # Type: String Stats: 759 properties of this type in this kind.

    - property: sequence
      external_name: sequence
      # Type: Integer Stats: 759 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: site_id
      external_name: site_id
      # Type: Integer Stats: 759 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: core_design
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key("core_design", key_is_id=True)

    - property: carousel_height
      external_name: carousel_height
      # Type: String Stats: 21 properties of this type in this kind.

    - property: carousel_width
      external_name: carousel_width
      # Type: String Stats: 21 properties of this type in this kind.

    - property: default
      external_name: default
      # Type: Boolean Stats: 21 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: displayname
      external_name: displayname
      # Type: String Stats: 21 properties of this type in this kind.

    - property: industry_id
      external_name: industry_id
      # Type: Integer Stats: 21 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: name
      external_name: name
      # Type: String Stats: 21 properties of this type in this kind.

    - property: structure
      external_name: structure
      # Type: String Stats: 21 properties of this type in this kind.


- kind: core_hours
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: _display
      external_name: _display
      # Type: String Stats: 104 properties of this type in this kind.

    - property: created
      external_name: created
      # Type: Date/Time Stats: 104 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: current
      external_name: current
      # Type: Boolean Stats: 104 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: dirty
      external_name: dirty
      # Type: Boolean Stats: 104 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: placeholder
      external_name: placeholder
      # Type: Boolean Stats: 104 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: private
      external_name: private
      # Type: Boolean Stats: 104 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: public
      external_name: public
      # Type: Boolean Stats: 104 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: reference
      external_name: reference
      # Type: String Stats: 104 properties of this type in this kind.

    - property: site_id
      external_name: site_id
      # Type: Integer Stats: 104 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: core_image
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: _cache
      external_name: _cache
      # Type: String Stats: 600 properties of this type in this kind.

    - property: content_type
      external_name: content_type
      # Type: String Stats: 600 properties of this type in this kind.

    - property: created
      external_name: created
      # Type: Date/Time Stats: 600 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: current
      external_name: current
      # Type: Boolean Stats: 600 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: dirty
      external_name: dirty
      # Type: Boolean Stats: 600 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: height
      external_name: height
      # Type: Integer Stats: 600 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: placeholder
      external_name: placeholder
      # Type: Boolean Stats: 600 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: private
      external_name: private
      # Type: Boolean Stats: 600 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: public
      external_name: public
      # Type: Boolean Stats: 600 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: reference
      external_name: reference
      # Type: String Stats: 600 properties of this type in this kind.

    - property: site_id
      external_name: site_id
      # Type: Integer Stats: 600 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: value
      external_name: value
      # Type: String Stats: 600 properties of this type in this kind.

    - property: width
      external_name: width
      # Type: Integer Stats: 600 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: core_industry
  connector: csv
  property_map:
    - property: __key__ 
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key("core_industry", key_is_id=True)

    - property: name
      external_name: name
      # Type: String Stats: 13 properties of this type in this kind.

    - property: vertical_id
      external_name: vertical_id
      # Type: Integer Stats: 13 properties of this type in this kind.
      import_transform: transform.create_foreign_key("core_vertical", key_is_id=True)
      export_transform: transform.none_if_empty(unicode)


- kind: core_location
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: address_1
      external_name: address_1
      # Type: String Stats: 144 properties of this type in this kind.

    - property: address_2
      external_name: address_2
      # Type: String Stats: 144 properties of this type in this kind.

    - property: address_county
      external_name: address_county
      # Type: String Stats: 144 properties of this type in this kind.

    - property: address_location
      external_name: address_location
      # Type: String Stats: 144 properties of this type in this kind.

    - property: created
      external_name: created
      # Type: Date/Time Stats: 144 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: current
      external_name: current
      # Type: Boolean Stats: 144 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: dirty
      external_name: dirty
      # Type: Boolean Stats: 144 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: placeholder
      external_name: placeholder
      # Type: Boolean Stats: 144 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: postcode
      external_name: postcode
      # Type: String Stats: 144 properties of this type in this kind.

    - property: private
      external_name: private
      # Type: Boolean Stats: 144 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: public
      external_name: public
      # Type: Boolean Stats: 144 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: reference
      external_name: reference
      # Type: String Stats: 144 properties of this type in this kind.

    - property: site_id
      external_name: site_id
      # Type: Integer Stats: 144 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: core_message
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: added_on
      external_name: added_on
      # Type: Date/Time Stats: 64 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: email
      external_name: email
      # Type: String Stats: 64 properties of this type in this kind.

    - property: message
      external_name: message
      # Type: Text Stats: 64 properties of this type in this kind.
      import_transform: db.Text

    - property: name
      external_name: name
      # Type: String Stats: 64 properties of this type in this kind.

    - property: new
      external_name: new
      # Type: Boolean Stats: 64 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: phone
      external_name: phone
      # Type: String Stats: 64 properties of this type in this kind.

    - property: site_id
      external_name: site_id
      # Type: Integer Stats: 64 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: core_pageseo
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: created
      external_name: created
      # Type: Date/Time Stats: 536 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: current
      external_name: current
      # Type: Boolean Stats: 536 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: description
      external_name: description
      # Type: Text Stats: 536 properties of this type in this kind.
      import_transform: db.Text

    - property: dirty
      external_name: dirty
      # Type: Boolean Stats: 536 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: placeholder
      external_name: placeholder
      # Type: Boolean Stats: 536 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: private
      external_name: private
      # Type: Boolean Stats: 536 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: public
      external_name: public
      # Type: Boolean Stats: 536 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: reference
      external_name: reference
      # Type: String Stats: 536 properties of this type in this kind.

    - property: site_id
      external_name: site_id
      # Type: Integer Stats: 536 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: tabname
      external_name: tabname
      # Type: String Stats: 536 properties of this type in this kind.

    - property: title
      external_name: title
      # Type: String Stats: 536 properties of this type in this kind.

    - property: url
      external_name: url
      # Type: String Stats: 536 properties of this type in this kind.


- kind: core_seo
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: created
      external_name: created
      # Type: Date/Time Stats: 98 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: current
      external_name: current
      # Type: Boolean Stats: 98 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: dirty
      external_name: dirty
      # Type: Boolean Stats: 98 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: long_description
      external_name: long_description
      # Type: Text Stats: 98 properties of this type in this kind.
      import_transform: db.Text

    - property: placeholder
      external_name: placeholder
      # Type: Boolean Stats: 98 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: private
      external_name: private
      # Type: Boolean Stats: 98 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: public
      external_name: public
      # Type: Boolean Stats: 98 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: reference
      external_name: reference
      # Type: String Stats: 98 properties of this type in this kind.

    - property: short_description
      external_name: short_description
      # Type: String Stats: 98 properties of this type in this kind.

    - property: site_id
      external_name: site_id
      # Type: Integer Stats: 98 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: core_seokeyword
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: _seo
      external_name: _seo
      # Type: String Stats: 425 properties of this type in this kind.

    - property: created
      external_name: created
      # Type: Date/Time Stats: 425 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: current
      external_name: current
      # Type: Boolean Stats: 425 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: dirty
      external_name: dirty
      # Type: Boolean Stats: 425 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: placeholder
      external_name: placeholder
      # Type: Boolean Stats: 425 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: private
      external_name: private
      # Type: Boolean Stats: 425 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: public
      external_name: public
      # Type: Boolean Stats: 425 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: reference
      external_name: reference
      # Type: String Stats: 425 properties of this type in this kind.

    - property: site_id
      external_name: site_id
      # Type: Integer Stats: 425 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: value
      external_name: value
      # Type: String Stats: 425 properties of this type in this kind.


- kind: core_site
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: administrator_id
      external_name: administrator_id
      # Type: Integer Stats: 121 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: created
      external_name: created
      # Type: Date/Time Stats: 121 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: domain
      external_name: domain
      # Type: String Stats: 121 properties of this type in this kind.

    - property: industry_id
      external_name: industry_id
      # Type: Integer Stats: 121 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: core_template
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: design_id
      external_name: design_id
      # Type: Integer Stats: 36 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: name
      external_name: name
      # Type: String Stats: 36 properties of this type in this kind.


- kind: core_text
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: created
      external_name: created
      # Type: Date/Time Stats: 1656 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: current
      external_name: current
      # Type: Boolean Stats: 1656 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: dirty
      external_name: dirty
      # Type: Boolean Stats: 1656 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: placeholder
      external_name: placeholder
      # Type: Boolean Stats: 1656 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: private
      external_name: private
      # Type: Boolean Stats: 1656 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: public
      external_name: public
      # Type: Boolean Stats: 1656 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: reference
      external_name: reference
      # Type: String Stats: 1656 properties of this type in this kind.

    - property: site_id
      external_name: site_id
      # Type: Integer Stats: 1656 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: value
      external_name: value
      # Type: Text Stats: 1656 properties of this type in this kind.
      import_transform: db.Text


- kind: core_vertical
  connector: csv 
  property_map:
    - property: __key__
      external_name: key 
      import_transform: transform.create_foreign_key("core_vertical", key_is_id=True)
      export_transform: transform.key_id_or_name_as_string

    - property: name
      external_name: name
      # Type: String Stats: 1 properties of this type in this kind.


- kind: django_admin_log
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: action_flag
      external_name: action_flag
      # Type: Integer Stats: 95 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: action_time
      external_name: action_time
      # Type: Date/Time Stats: 95 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: change_message
      external_name: change_message
      # Type: Text Stats: 95 properties of this type in this kind.
      import_transform: db.Text

    - property: content_type_id
      external_name: content_type_id
      # Type: Integer Stats: 95 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: object_id
      external_name: object_id
      # Type: Text Stats: 95 properties of this type in this kind.
      import_transform: db.Text

    - property: object_repr
      external_name: object_repr
      # Type: String Stats: 95 properties of this type in this kind.

    - property: user_id
      external_name: user_id
      # Type: Integer Stats: 95 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: django_content_type
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: app_label
      external_name: app_label
      # Type: String Stats: 27 properties of this type in this kind.

    - property: model
      external_name: model
      # Type: String Stats: 27 properties of this type in this kind.

    - property: name
      external_name: name
      # Type: String Stats: 27 properties of this type in this kind.


- kind: django_session
  connector: csv 
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: expire_date
      external_name: expire_date
      # Type: Date/Time Stats: 283 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: session_data
      external_name: session_data
      # Type: Text Stats: 283 properties of this type in this kind.
      import_transform: db.Text


- kind: gallery_photo
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: _description
      external_name: _description
      # Type: String Stats: 126 properties of this type in this kind.

    - property: _image
      external_name: _image
      # Type: String Stats: 126 properties of this type in this kind.

    - property: _project
      external_name: _project
      # Type: String Stats: 126 properties of this type in this kind.

    - property: _title
      external_name: _title
      # Type: String Stats: 126 properties of this type in this kind.

    - property: created
      external_name: created
      # Type: Date/Time Stats: 126 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: current
      external_name: current
      # Type: Boolean Stats: 126 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: dirty
      external_name: dirty
      # Type: Boolean Stats: 126 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: placeholder
      external_name: placeholder
      # Type: Boolean Stats: 126 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: private
      external_name: private
      # Type: Boolean Stats: 126 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: public
      external_name: public
      # Type: Boolean Stats: 126 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: reference
      external_name: reference
      # Type: String Stats: 126 properties of this type in this kind.

    - property: sequence
      external_name: sequence
      # Type: Integer Stats: 126 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: site_id
      external_name: site_id
      # Type: Integer Stats: 126 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: gallery_portfolio
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: created
      external_name: created
      # Type: Date/Time Stats: 23 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: current
      external_name: current
      # Type: Boolean Stats: 23 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: dirty
      external_name: dirty
      # Type: Boolean Stats: 23 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: placeholder
      external_name: placeholder
      # Type: Boolean Stats: 23 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: private
      external_name: private
      # Type: Boolean Stats: 23 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: public
      external_name: public
      # Type: Boolean Stats: 23 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: reference
      external_name: reference
      # Type: String Stats: 23 properties of this type in this kind.

    - property: site_id
      external_name: site_id
      # Type: Integer Stats: 23 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: gallery_project
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: _description
      external_name: _description
      # Type: String Stats: 58 properties of this type in this kind.

    - property: _portfolio
      external_name: _portfolio
      # Type: String Stats: 58 properties of this type in this kind.

    - property: _title
      external_name: _title
      # Type: String Stats: 58 properties of this type in this kind.

    - property: created
      external_name: created
      # Type: Date/Time Stats: 58 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: current
      external_name: current
      # Type: Boolean Stats: 58 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: dirty
      external_name: dirty
      # Type: Boolean Stats: 58 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: placeholder
      external_name: placeholder
      # Type: Boolean Stats: 58 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: private
      external_name: private
      # Type: Boolean Stats: 58 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: public
      external_name: public
      # Type: Boolean Stats: 58 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: reference
      external_name: reference
      # Type: String Stats: 58 properties of this type in this kind.

    - property: sequence
      external_name: sequence
      # Type: Integer Stats: 58 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: site_id
      external_name: site_id
      # Type: Integer Stats: 58 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: news_article
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: _content
      external_name: _content
      # Type: String Stats: 143 properties of this type in this kind.

    - property: _date
      external_name: _date
      # Type: String Stats: 143 properties of this type in this kind.

    - property: _feed
      external_name: _feed
      # Type: String Stats: 143 properties of this type in this kind.

    - property: _title
      external_name: _title
      # Type: String Stats: 143 properties of this type in this kind.

    - property: created
      external_name: created
      # Type: Date/Time Stats: 143 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: current
      external_name: current
      # Type: Boolean Stats: 143 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: dirty
      external_name: dirty
      # Type: Boolean Stats: 143 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: placeholder
      external_name: placeholder
      # Type: Boolean Stats: 143 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: private
      external_name: private
      # Type: Boolean Stats: 143 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: public
      external_name: public
      # Type: Boolean Stats: 143 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: reference
      external_name: reference
      # Type: String Stats: 143 properties of this type in this kind.

    - property: site_id
      external_name: site_id
      # Type: Integer Stats: 143 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: news_feed
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: created
      external_name: created
      # Type: Date/Time Stats: 27 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: current
      external_name: current
      # Type: Boolean Stats: 27 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: dirty
      external_name: dirty
      # Type: Boolean Stats: 27 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: placeholder
      external_name: placeholder
      # Type: Boolean Stats: 27 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: private
      external_name: private
      # Type: Boolean Stats: 27 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: public
      external_name: public
      # Type: Boolean Stats: 27 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: reference
      external_name: reference
      # Type: String Stats: 27 properties of this type in this kind.

    - property: site_id
      external_name: site_id
      # Type: Integer Stats: 27 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: testimonials_feedback
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: created
      external_name: created
      # Type: Date/Time Stats: 21 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: current
      external_name: current
      # Type: Boolean Stats: 21 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: dirty
      external_name: dirty
      # Type: Boolean Stats: 21 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: placeholder
      external_name: placeholder
      # Type: Boolean Stats: 21 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: private
      external_name: private
      # Type: Boolean Stats: 21 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: public
      external_name: public
      # Type: Boolean Stats: 21 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: reference
      external_name: reference
      # Type: String Stats: 21 properties of this type in this kind.

    - property: site_id
      external_name: site_id
      # Type: Integer Stats: 21 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: testimonials_testimonial
  connector: csv
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: _content
      external_name: _content
      # Type: String Stats: 128 properties of this type in this kind.

    - property: _date
      external_name: _date
      # Type: String Stats: 128 properties of this type in this kind.

    - property: _feedback
      external_name: _feedback
      # Type: String Stats: 128 properties of this type in this kind.

    - property: _name
      external_name: _name
      # Type: String Stats: 128 properties of this type in this kind.

    - property: created
      external_name: created
      # Type: Date/Time Stats: 128 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: current
      external_name: current
      # Type: Boolean Stats: 128 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: dirty
      external_name: dirty
      # Type: Boolean Stats: 128 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: placeholder
      external_name: placeholder
      # Type: Boolean Stats: 128 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: private
      external_name: private
      # Type: Boolean Stats: 128 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: public
      external_name: public
      # Type: Boolean Stats: 128 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: reference
      external_name: reference
      # Type: String Stats: 128 properties of this type in this kind.

    - property: site_id
      external_name: site_id
      # Type: Integer Stats: 128 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

